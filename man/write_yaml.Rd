% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{write_yaml}
\alias{write_yaml}
\title{Write an R object as YAML 1.2 to a file.}
\usage{
write_yaml(value, path = NULL, multi = FALSE)
}
\arguments{
\item{value}{Any R object composed of lists, atomic vectors, and scalars.}

\item{path}{Scalar string file path to write YAML to. When \code{NULL} (the default),
write to R's standard output connection.}

\item{multi}{When \code{TRUE}, treat \code{value} as a list of YAML documents and encode a stream.}
}
\value{
Invisibly returns \code{value}.
}
\description{
Writes the YAML stream to a file or stdout and always emits explicit document
start (\verb{---}) markers and a final end (\code{...}) marker. Respects \code{yaml_tag}
attributes when encoding values.
}
\examples{
write_yaml(list(foo = 1, bar = list(2, "baz")))

write_yaml(list("foo", "bar"), multi = TRUE)

tagged <- structure("1 + 1", yaml_tag = "!expr")
write_yaml(tagged)
}
